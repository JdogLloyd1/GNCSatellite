git add -A .
git commit -m 'message'
git push


git fetch
git pull

gh issue create
gh issue create --title "asdf" --body "more details"

git submodule add --url-- --Folder name--

when Git throws empty object or loose object corrupt errors: 

find .git/objects/ -size 0 -exec rm -f {} \;
git fetch origin 

then git pull to merge if needed
